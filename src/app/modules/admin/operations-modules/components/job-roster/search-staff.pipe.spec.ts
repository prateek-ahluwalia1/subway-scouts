import { SearchStaffPipe } from './search-staff.pipe';

describe('SearchStaffPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchStaffPipe();
    expect(pipe).toBeTruthy();
  });
});
